{
  "name": "gleam",
  "version": "1.0.0",
  "scripts": {
    "format": "prettier --write '**/*.{css,js,html}'",
    "check-format": "prettier --check '**/*.{css,js,html}'"
  },
  "dependencies": {
    "prettier": "^2.7.1"
  }
}
